# pnpm configuration — ClauseWise

# Allow native build scripts for these packages (pnpm v11+ syntax)
# esbuild: required by Next.js bundler
# sharp: Next.js image optimization
# unrs-resolver: pnpm internal tooling
allow-build=esbuild,sharp,unrs-resolver
